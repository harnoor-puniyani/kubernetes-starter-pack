kind: ReplicaSet
apiVersion: apps/v1
metadata:
  name: nginx
  namespace: nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      name: nginx-replicaset-pod
      labels:
        app: nginx
    spec:
      containers:
        - name: nginxrep
          image: nginx:latest
          ports:
            - containerPort: 80

